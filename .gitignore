# secrets
auth.json
auth.json.bak
# caches and runtime state
sessions/
plans/
models-store.json
mcp-cache.json
mcp-npx-cache.json
web-search-cache/
trust.json
powerline-footer/
*.log
# installed packages (restored with `pi install` from settings.json)
npm/node_modules/
npm/package-lock.json
git/
# extensions' own npm deps pi auto-installs at the repo root on first missing-module load
node_modules/
# backups and scratch
*.bak
__pycache__/
mcp.json
extensions/pi-permission-system/logs/

# serena leftover, caches, suffixed backups
.serena/
mcp-cache.json.*
models.json.bak-*
settings.json.bak-*

# per-machine voice dictation state (hint session counter)
voice.json

# patches/apply.mjs fingerprint of the last clean apply (per machine)
patches/.applied-stamp
# backups of gitignored or generated files
mcp.json.bak*
*.bak-*
